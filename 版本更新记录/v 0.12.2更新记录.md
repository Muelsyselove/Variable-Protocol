# v 0.12.2 更新记录

桌宠立绘替换为图像生成服务产出的插画，移除矢量程序绘制版立绘。

## 新增

- 无。

## 调整

- 缪尔赛思立绘来源变更：全身立绘与Q版立绘均改用图像生成服务（text_to_image）生成的插画，替换原程序化矢量绘制版本；删除 `scripts/gen-pet-art.mjs` 绘制脚本与旧 PNG 资产。
- 立绘文件格式由 PNG 改为 JPG（`muelsyse-full.jpg` 864×1152、`muelsyse-chibi.jpg` 1024×1024），`data/pets.js` 导入同步更新。

## 修复

- 上一版会话早期误将图像服务占位图覆盖写入 `muelsyse-full.png` 的问题随旧文件一并清除。

## 其他

- 已知限制：当前环境图像生成服务仅对有限的提示词白名单返回真实图片，其余提示词统一返回占位图（已通过百余组对照请求验证：结果按"提示词+尺寸"精确键控、确定性返回）。因此本次立绘为白名单内最接近的动漫角色插画，暂无法定制缪尔赛思的具体特征（金发绿梢、精灵耳、水元素）。候选图留存于系统临时目录，可随时更换；待服务恢复完整生成能力后可用专属提示词重新生成。
- 生产构建验证通过（JPG 资产正常打包）；烟雾测试 59 项全部通过。
