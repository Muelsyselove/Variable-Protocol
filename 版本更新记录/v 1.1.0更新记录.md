# v 1.1.0 更新记录

> 打包版（安装发行版）新档开局赠礼：首次建档直接赠送 **1000 算力币**。

## 新增

### 打包版开局赠礼

- 打包版启动时主进程为渲染层附加 `pkg=1` 发行版标记（主窗口与桌宠窗统一携带）。
- 首次建档（无 `profile.json` 存档）且检测到发行版标记时，初始算力币设为 1000，开局即可在桌宠终端购买宠物与口粮。

## 修复

- 无。

## 调整

- 仅打包版生效：开发版新档仍为 0 算力币，不影响开发测试。
- 仅首次建档生效：已有存档（含 v1.0.0 升级用户）不重复赠送。
- 主菜单版本标识更新为 `PROTOCOL TERMINAL v1.1.0`。
- 窗口图标改为从 asar 外的 `resources\icon.png` 读取（该调整未解决任务栏空白问题，真正修复见 v 1.1.1）。

## 其他

- 版本号 v1.0.0 → v1.1.0（功能性更新）。
