# v 0.10.1 更新记录

修复AI接口连接HTTP 404问题。

## 新增

- 接口地址填写提示：设置界面AI接口说明更新，注明可直接填Base URL（自动补全路径）并给出常见服务商示例。

## 调整

- 无。

## 修复

- AI接口HTTP 404：此前主进程将用户填写的接口地址原样作为请求URL，填写Base URL（如 `https://api.openai.com/v1`、智谱 `…/api/paas/v4`）时POST目标缺少 `/chat/completions` 路径导致404。现对地址做自动规范化——已含完整路径则原样使用；以版本段（/v1、/v4、/v1beta等）结尾则补全 `/chat/completions`；裸域名（如 DeepSeek、Ollama本地地址）则补全 `/v1/chat/completions`；自动去除尾部斜杠。8种主流服务商地址格式验证全部通过。
- 错误提示增强：连接失败时显示实际请求的完整URL，便于排查地址问题。

## 其他

- 烟雾测试47项全部通过；生产构建验证通过。
