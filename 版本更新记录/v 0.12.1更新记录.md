# v 0.12.1 更新记录

修复桌宠终端界面打开后空白的问题。

## 新增

- 无。

## 调整

- 无。

## 修复

- 桌宠终端空白：`petshop.js` 模板中引用了 `SATIETY_DECAY_MINUTES`（口粮说明文案）但未从 `data/pets.js` 导入，渲染时抛出 ReferenceError 导致整个屏幕中断。已补全导入。同时核对全部引用 pets.js 导出的文件（gameover/map/menu），导入均完整无同类问题。

## 其他

- 生产构建验证通过。
