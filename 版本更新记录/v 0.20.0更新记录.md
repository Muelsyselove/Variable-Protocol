# v 0.20.0 更新记录

缪尔赛思大立绘重绘：MCP图像服务生成，2K级别分辨率+透明背景。

## 新增

- 无。

## 调整

- 缪尔赛思全身大立绘更换：由 MCP 图像生成服务（mcp_ai-image/generate_image）重新生成，分辨率 1024×1536 → **2048×3072**（2K级），背景由深青色场景图改为**透明背景**（RGBA）。桌宠终端图鉴与相关展示位自动使用新图。

## 修复

- 无。

## 其他

- 生成与处理管线（可复用）：
  1. MCP 生图：直接生成无法输出透明通道（RGB），采用色度键控策略——提示词指定纯品红背景（与角色绿/青/白/金配色零冲突，服务商输出全场 rgb(248,6,249) 纯净品红）；
  2. 色度键控抠图（`scripts/chroma-matte.mjs`）：键色估计→洪泛连通移除（61.8%背景）→边缘品红污染反解（alpha解算+前景色还原，23453像素）→内部残留清理（398795像素）；
  3. 数值验证（`scripts/verify-matte.mjs`）：四边角全透明、中心区主体保留79.5%、人形轮廓网格完整（头部/躯干/腿主轴清晰，周围散点为水滴与液体丝带特效）。
- 辅助脚本沉淀：`scripts/analyze-bg.mjs`、`analyze-bg2.mjs`（背景结构分析）、`chroma-matte.mjs`（色度键控抠图）、`verify-matte.mjs`（抠图质量数值验证）、`process-portrait.mjs`（渐变背景抠图+Lanczos放大，备用方案）。
- 生产构建验证通过（新立绘6.28MB正常打包）；烟雾测试59项全部通过。
- 注：Q版状态立绘（10张）仍为原版1024级；如需同步升级为透明底2K版本，可按同管线批量处理（提示词需指定品红背景重新生成）。
